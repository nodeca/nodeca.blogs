bundles:
  frontend:
    - blogs


packages:
  admin:
    entries:
      - client/admin
      - server/admin

  blogs:
    depends:
      - common

    entries:
      - client/blogs
      - server/blogs
