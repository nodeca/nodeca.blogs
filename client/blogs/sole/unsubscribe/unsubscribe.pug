- var user = self.users[self.user_id] || {};
- var to_type = '$$ N.models.users.Subscription.types.NORMAL $$'

#content.blogs-sole-unsubscribe(data-type=to_type, data-user-id=self.user_id)
  != self.partial('@common.blocks.navbar.logo_apps', { search_tab: 'blog_entries' })

  .layout__container.container-fluid
    != self.partial('@common.blocks.announces')

    header.page-head
      h1.page-head__title.page-loading__content(itemprop='name')= self.t('title')
      .icon.icon-loading-placeholder.page-loading__progress

    - var url = self.link_to('blogs.sole', { user_hid: self.user_id });
    - var link = '<a href="' + pug.escape(url) + '">' + pug.escape(user && user.nick) + '</a>';
    .page-loading__content!= self.t('message', { link: link })
