.editor-content-tags(
  class=self.tags.length === 0 ? 'editor-content-tags__m-empty' : ''
  data-on-click=self.apiPath + ':tags_edit'
)
  if self.tags.length
    .editor-content-tags__header= self.t('tags')

    ul.editor-content-tags__list
      each tag in self.tags
        li.editor-content-tags__item= tag
  else
    .editor-content-tags__header= self.t('click_to_add_tags')
